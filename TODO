- add way to choose activity without the keyboard
- add screensaver detection
- use additions/deletions/changes instead of model resets
- hook the model classes into RecordManager to avoid extra instantiations
