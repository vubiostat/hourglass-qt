#ifndef VIEW_H
#define VIEW_H

#define VIEW_PATH "@hourglass_VIEW_PATH@"

#include <QObject>
#include <QString>

#include "teng.h"
#include "tengwriter.h"
#include "tengerror.h"
#include "variables.h"
#include "activity.h"

class View : public QObject
{
  Q_OBJECT

  public:
    View(QString name, bool useLayout = true, QObject *parent = 0);
    QString render(VariableMap &variables);

  private:
    QString name;
    bool useLayout;
};

#endif
