#ifndef VIEW_H
#define VIEW_H

#define VIEW_PATH "@hourglass_VIEW_PATH@"

#include <QObject>
#include <QString>

#include "teng.h"
#include "tengwriter.h"
#include "tengerror.h"

class View : public QObject
{
  Q_OBJECT

  public:
    View(QString name, QObject *parent = 0);
    void addVariable(const QString &name, const QString &str);
    QString render();

  private:
    QString name;
    Teng::Fragment_t rootFragment;
};

#endif
